<% include ../partials/header.ejs %>

    <div class="container">
        <div class="row">
            <div class="col-md-offset-3 col-md-6">
                <h1><%= title %></h1>
                <a class="btn btn-primary" href="/user-list/add" role="button">
                <i class="fas fa-plus-circle"></i> Add a User</a>
                <br>
                <table class="table table-bordered table-striped table-hover">
                    <!-- Header Row-->
                    <tr>
                        <th class="text-center">First Name</th>
                        <th class="text-center">Last Name</th>
                        <th class="text-center">Age</th>
                        <th class="text-center"></th>
                        <th class="text-center"></th>
                    </tr>
                     <!-- Repeatable Template Row -->
                     <% for(let count = 0; count < userList.length; count++) { %>
                        <tr>
                            <td class="text-center"><%= userList[count].firstName %></td>
                            <td class="text-center"><%= userList[count].lastName %></td>
                            <td class="text-center"><%= userList[count].age %></td>

                            <td class="text-center">
                            <a href="/user-list/edit/<%= userList[count]._id %>"
                                class="btn btn-primary"><i class="fas fa-pencil-alt"></i> Edit</a>
                            </td>

                            <td class="text-center">
                                    <a href="/user-list/delete/<%= userList[count]._id %>"
                                        class="btn btn-danger"><i class="fas fa-trash-alt"></i> Delete</a>
                                    </td>
                        </tr>
                    <% } %>
                </table>

            </div>
        </div>
    </div>

<% include ../partials/footer.ejs %>